# Flutter Build & Share Automation Script

This script automates:

1. Flutter build
2. IPA/APK generation
3. Uploading build to Loadly
4. Returning instant install link

Usage:

Android:
./build.sh --a

iOS:
./build.sh --i
